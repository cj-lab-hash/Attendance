{
  "name": "chronotrack-checker",
  "version": "1.0.0",
  "description": "Simple site + Node server that checks ChronoTrack logs for employee IDs (requires login/cookie)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node ./node_modules/playwright/cli.js install --with-deps chromium"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7",
    "cheerio": "^1.0.0-rc.12",
    "playwright": "^1.55.0"
  },
  "devDependencies": {}
}
